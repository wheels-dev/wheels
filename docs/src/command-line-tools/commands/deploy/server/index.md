# `wheels deploy server`

Host-level operations that don't fit into the app/proxy/accessory model.

## Verbs

| Verb | Summary |
|------|---------|
| [`exec`](./exec.md) | Run an arbitrary shell command on one or all hosts. |
| [`bootstrap`](./bootstrap.md) | Install Docker on a fresh host (idempotent). |
