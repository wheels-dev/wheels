I want to test a grouping of the  wheels cli commands: $ARGUMENTS

1. Look at the guides/command-line-tools/commands/README.md to find the $ARGUMENTS commands

2. Build a plan of testing each command in the grouping and use /project:cli:test to test each command.
