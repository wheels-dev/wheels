---
title: CLI Reference
description: The wheels binary — two command surfaces (LuCLI core + Wheels Module) in one install.
type: reference
sidebar:
  order: 1
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';

The `wheels` binary is the single entry point for everything you do with a Wheels app from the terminal — scaffolding a new project, generating code, running migrations, running tests, starting the dev server, inspecting routes, driving the REPL. One install, one name on your `$PATH`.

**You'll use this for:**

- Creating a new Wheels project and scaffolding models, controllers, views, and migrations.
- Your daily dev loop — `wheels generate`, `wheels migrate`, `wheels test`, `wheels server` — without leaving the shell.
- Inspecting and diagnosing an app you didn't write — routes, stats, dependency checks, and the built-in REPL.

## Two surfaces in one command

The v4 `wheels` binary is [LuCLI](https://github.com/lucee/LuCLI) renamed, with a Wheels Module bolted on. LuCLI is a general-purpose CFML command runner — it provides the server lifecycle, REPL, CFML execution, system and secrets helpers, module and dependency management, AI helpers, and shell completion. The Wheels Module adds the framework-specific commands — `generate`, `migrate`, `seed`, `test`, `routes`, `console`, and the rest. Both surfaces dispatch from the same binary:

- `wheels generate model User` — Wheels Module command.
- `wheels server start` — LuCLI core command.

You don't install them separately, and you don't switch contexts between them. Everything below is namespaced by which surface it comes from so you know where a given feature lives, but from your terminal it's all `wheels <subcommand>`.

## Getting started

<CardGrid>
  <LinkCard
    title="Installation"
    href="./installation/"
    description="Homebrew, Chocolatey, manual tarball, and PATH sanity checks."
  />
  <LinkCard
    title="Quick Start"
    href="./quick-start/"
    description="From a fresh shell to a running Wheels app in under five minutes."
  />
  <LinkCard
    title="Configuration"
    href="./configuration/"
    description="How LuCLI and the Wheels Module are configured — profiles, env, module.json."
  />
</CardGrid>

## Wheels commands

Framework-specific commands contributed by the Wheels Module. These are what you reach for daily.

<CardGrid>
  <LinkCard
    title="Creating a Project"
    href="./wheels-commands/creating-a-project/"
    description="wheels new and wheels create app — scaffolding a fresh Wheels project."
  />
  <LinkCard
    title="Code Generation"
    href="./wheels-commands/code-generation/"
    description="wheels generate — models, controllers, views, migrations, scaffolds, routes, tests."
  />
  <LinkCard
    title="Database"
    href="./wheels-commands/database/"
    description="wheels migrate, wheels seed, and wheels db — schema and seed data workflows."
  />
  <LinkCard
    title="Dev Server"
    href="./wheels-commands/dev-server/"
    description="wheels start, wheels stop, wheels reload — running your app locally."
  />
  <LinkCard
    title="Testing"
    href="./wheels-commands/testing/"
    description="wheels test and wheels browser — running specs and the Playwright-driven browser suite."
  />
  <LinkCard
    title="App Inspection"
    href="./wheels-commands/app-inspection/"
    description="wheels routes, wheels info, wheels stats, wheels notes — reading the shape of an app."
  />
  <LinkCard
    title="Code Quality"
    href="./wheels-commands/code-quality/"
    description="wheels analyze, wheels validate, wheels doctor — lint and health checks."
  />
  <LinkCard
    title="Scaffold Cleanup"
    href="./wheels-commands/scaffold-cleanup/"
    description="wheels destroy — undo a generator cleanly."
  />
  <LinkCard
    title="Console & REPL"
    href="./wheels-commands/console-and-repl/"
    description="wheels console — interactive CFML with your app loaded."
  />
  <LinkCard
    title="Upgrade"
    href="./wheels-commands/upgrade/"
    description="wheels upgrade — scan for breaking changes before moving to a new framework version."
  />
</CardGrid>

## Core commands (LuCLI)

General-purpose commands inherited from LuCLI. Available in any CFML project, Wheels or not.

<CardGrid>
  <LinkCard
    title="Server"
    href="./core-commands/server/"
    description="lucli server — the persistent CFML server process that serves your app."
  />
  <LinkCard
    title="CFML Execution"
    href="./core-commands/cfml-execution/"
    description="lucli cfml and lucli run — one-shot CFML evaluation and script execution."
  />
  <LinkCard
    title="System & Secrets"
    href="./core-commands/system-and-secrets/"
    description="lucli system and lucli secrets — inspect the runtime and manage per-profile secrets."
  />
  <LinkCard
    title="Modules & Deps"
    href="./core-commands/modules-and-deps/"
    description="lucli modules and lucli deps — installing modules and managing dependencies."
  />
  <LinkCard
    title="AI & Completion"
    href="./core-commands/ai-and-completion/"
    description="lucli ai and lucli completion — AI helpers and shell completion setup."
  />
</CardGrid>

## MCP integration

The `wheels` binary exposes its commands as MCP tools so an AI agent can drive the same operations you run from the terminal. `wheels mcp wheels` is the canonical stdio surface — auto-discovered from `cli/lucli/Module.cfc` and usable from any MCP-aware client. A handful of CLI-only commands (`start`, `stop`, `new`, `console`, `browser`, `d`, and the `mcp` meta command itself) are hidden from the MCP tool list because their semantics don't translate to a single tool call. A legacy HTTP endpoint at `/wheels/mcp` still exists for backward compatibility but is deprecated — prefer the stdio surface for new integrations. See [MCP Integration](./mcp-integration/) for setup, the full tool catalog, and the deprecation timeline.
