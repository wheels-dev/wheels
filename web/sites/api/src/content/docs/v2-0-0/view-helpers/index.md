---
title: View Helpers
description: "59 View Helpers functions in Wheels 2.0.0."
---

- [`authenticityTokenField()`](/v2-0-0/view-helpers/authenticitytokenfield/) — Returns a hidden form field containing a new authenticity token.
- [`autoLink()`](/v2-0-0/view-helpers/autolink/) — Turns all URLs and email addresses into links.
- [`buttonTag()`](/v2-0-0/view-helpers/buttontag/) — Builds and returns a string containing a button form control.
- [`buttonTo()`](/v2-0-0/view-helpers/buttonto/) — Creates a form containing a single button that submits to the URL.
- [`checkBox()`](/v2-0-0/view-helpers/checkbox/) — Builds and returns a string containing a check box form control based on the supplied name.
- [`checkBoxTag()`](/v2-0-0/view-helpers/checkboxtag/) — Builds and returns a string containing a check box form control based on the supplied name.
- [`contentFor()`](/v2-0-0/view-helpers/contentfor/) — Used to store a section's output for rendering within a layout.
- [`contentForLayout()`](/v2-0-0/view-helpers/contentforlayout/) — Includes content for the body section, which equates to the output generated by the view template run by the request.
- [`csrfMetaTags()`](/v2-0-0/view-helpers/csrfmetatags/) — Include this in your layouts' <code>head</code> sections to include meta tags containing the authenticity token for use 
- [`cycle()`](/v2-0-0/view-helpers/cycle/) — Cycles through list values every time it is called.
- [`dateSelect()`](/v2-0-0/view-helpers/dateselect/) — Builds and returns a string containing three <code>select</code> form controls for month, day, and year based on the sup
- [`dateSelectTags()`](/v2-0-0/view-helpers/dateselecttags/) — Builds and returns a string containing three <code>select</code> form controls (month, day, and year) based on a name an
- [`dateTimeSelect()`](/v2-0-0/view-helpers/datetimeselect/) — Builds and returns a string containing six <code>select</code> form controls (three for date selection and the remaining
- [`dateTimeSelectTags()`](/v2-0-0/view-helpers/datetimeselecttags/) — Builds and returns a string containing six <code>select</code> form controls (three for date selection and the remaining
- [`daySelectTag()`](/v2-0-0/view-helpers/dayselecttag/) — Builds and returns a string containing a <code>select</code> form control for the days of the week based on the supplied
- [`endFormTag()`](/v2-0-0/view-helpers/endformtag/) — Builds and returns a string containing the closing <code>form</code> tag.
- [`errorMessageOn()`](/v2-0-0/view-helpers/errormessageon/) — Returns the error message, if one exists, on the object's property.
- [`errorMessagesFor()`](/v2-0-0/view-helpers/errormessagesfor/) — Builds and returns a list (<code>ul</code> tag with a default <code>class</code> of <code>error-messages</code>) contain
- [`fileField()`](/v2-0-0/view-helpers/filefield/) — Builds and returns a string containing a file field form control based on the supplied objectName and property.
- [`fileFieldTag()`](/v2-0-0/view-helpers/filefieldtag/) — Builds and returns a string containing a file form control based on the supplied name.
- [`flashMessages()`](/v2-0-0/view-helpers/flashmessages/) — Displays a marked-up listing of messages that exist in the Flash.
- [`hasManyCheckBox()`](/v2-0-0/view-helpers/hasmanycheckbox/) — Used as a shortcut to output the proper form elements for an association.
- [`hasManyRadioButton()`](/v2-0-0/view-helpers/hasmanyradiobutton/) — Used as a shortcut to output the proper form elements for an association.
- [`hiddenField()`](/v2-0-0/view-helpers/hiddenfield/) — Builds and returns a string containing a hidden field form control based on the supplied objectName and property.
- [`hiddenFieldTag()`](/v2-0-0/view-helpers/hiddenfieldtag/) — Builds and returns a string containing a hidden field form control based on the supplied name.
- [`highlight()`](/v2-0-0/view-helpers/highlight/) — Highlights the phrase(s) everywhere in the text if found by wrapping them in <code>span</code> tags.
- [`hourSelectTag()`](/v2-0-0/view-helpers/hourselecttag/) — Builds and returns a string containing one <code>select</code> form control for the hours of the day based on the suppli
- [`imageTag()`](/v2-0-0/view-helpers/imagetag/) — Returns an <code>img</code> tag.
- [`includeContent()`](/v2-0-0/view-helpers/includecontent/) — Used to output the content for a particular section in a layout.
- [`includedInObject()`](/v2-0-0/view-helpers/includedinobject/) — Used as a shortcut to check if the specified IDs are a part of the main form object.
- [`includeLayout()`](/v2-0-0/view-helpers/includelayout/) — Includes the contents of another layout file.
- [`includePartial()`](/v2-0-0/view-helpers/includepartial/) — Includes the specified partial file in the view.
- [`javaScriptIncludeTag()`](/v2-0-0/view-helpers/javascriptincludetag/) — Returns a <code>script</code> tag for a JavaScript file (or several) based on the supplied arguments.
- [`linkTo()`](/v2-0-0/view-helpers/linkto/) — Creates a link to another page in your application.
- [`mailTo()`](/v2-0-0/view-helpers/mailto/) — Creates a <code>mailto</code> link tag to the specified email address, which is also used as the name of the link unless
- [`minuteSelectTag()`](/v2-0-0/view-helpers/minuteselecttag/) — Builds and returns a string containing one <code>select</code> form control for the minutes of an hour based on the supp
- [`monthSelectTag()`](/v2-0-0/view-helpers/monthselecttag/) — Builds and returns a string containing a <code>select</code> form control for the months of the year based on the suppli
- [`paginationLinks()`](/v2-0-0/view-helpers/paginationlinks/) — Builds and returns a string containing links to pages based on a paginated query.
- [`passwordField()`](/v2-0-0/view-helpers/passwordfield/) — Builds and returns a string containing a password field form control based on the supplied objectName and property.
- [`passwordFieldTag()`](/v2-0-0/view-helpers/passwordfieldtag/) — Builds and returns a string containing a password field form control based on the supplied name.
- [`radioButton()`](/v2-0-0/view-helpers/radiobutton/) — Builds and returns a string containing a radio button form control based on the supplied objectName and property.
- [`radioButtonTag()`](/v2-0-0/view-helpers/radiobuttontag/) — Builds and returns a string containing a radio button form control based on the supplied name.
- [`resetCycle()`](/v2-0-0/view-helpers/resetcycle/) — Resets a cycle so that it starts from the first list value the next time it is called.
- [`secondSelectTag()`](/v2-0-0/view-helpers/secondselecttag/) — Builds and returns a string containing one <code>select</code> form control for the seconds of a minute based on the sup
- [`select()`](/v2-0-0/view-helpers/select/) — Builds and returns a string containing a select form control based on the supplied objectName and property.
- [`selectTag()`](/v2-0-0/view-helpers/selecttag/) — Builds and returns a string containing a select form control based on the supplied name and options.
- [`simpleFormat()`](/v2-0-0/view-helpers/simpleformat/) — Returns formatted text using HTML break tags (<code><br></code>) and HTML paragraph elements (<code><p></p></code>) base
- [`startFormTag()`](/v2-0-0/view-helpers/startformtag/) — Builds and returns a string containing the opening <code>form</code> tag.
- [`stripLinks()`](/v2-0-0/view-helpers/striplinks/) — Removes all links from an HTML string, leaving just the link text.
- [`stripTags()`](/v2-0-0/view-helpers/striptags/) — Removes all HTML tags from a string.
- [`styleSheetLinkTag()`](/v2-0-0/view-helpers/stylesheetlinktag/) — Returns a <code>link</code> tag for a stylesheet (or several) based on the supplied arguments.
- [`submitTag()`](/v2-0-0/view-helpers/submittag/) — Builds and returns a string containing a submit button form control.
- [`textArea()`](/v2-0-0/view-helpers/textarea/) — Builds and returns a string containing a text area field form control based on the supplied objectName and property.
- [`textAreaTag()`](/v2-0-0/view-helpers/textareatag/) — Builds and returns a string containing a text area form control based on the supplied name.
- [`textField()`](/v2-0-0/view-helpers/textfield/) — Builds and returns a string containing a text field form control based on the supplied objectName and property.
- [`textFieldTag()`](/v2-0-0/view-helpers/textfieldtag/) — Builds and returns a string containing a text field form control based on the supplied name.
- [`timeSelect()`](/v2-0-0/view-helpers/timeselect/) — Builds and returns a string containing three <code>select</code> form controls for hour, minute, and second based on the
- [`timeSelectTags()`](/v2-0-0/view-helpers/timeselecttags/) — Builds and returns a string containing three <code>select</code> form controls for hour, minute, and second based on nam
- [`yearSelectTag()`](/v2-0-0/view-helpers/yearselecttag/) — Builds and returns a string containing a <code>select</code> form control for a range of years based on the supplied nam
