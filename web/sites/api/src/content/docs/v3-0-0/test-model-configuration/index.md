---
title: Test Model Configuration
description: "4 Test Model Configuration functions in Wheels 3.0.0."
---

- [`packageSetup()`](/v3-0-0/test-model-configuration/packagesetup/) — The packageSetup() function is a callback in Wheels’ legacy testing framework. It runs once before the first test case i
- [`packageTeardown()`](/v3-0-0/test-model-configuration/packageteardown/) — The packageTeardown() function is a callback in Wheels’ legacy testing framework. It runs once after the last test case 
- [`setup()`](/v3-0-0/test-model-configuration/setup/) — Callback used in Wheels legacy testing framework. It runs before every individual test case within a test suite. This al
- [`teardown()`](/v3-0-0/test-model-configuration/teardown/) — Callback that executes after every test case when using Wheels’ legacy testing framework. It is typically used to clean 
