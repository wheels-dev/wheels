---
title: Global Helpers
description: "19 Global Helpers functions in Wheels 3.0.0."
---

- [`capitalize()`](/v3-0-0/global-helpers/capitalize/) — Capitalizes the first letter of every word in the provided text, creating a nicely formatted title or sentence.
- [`controller()`](/v3-0-0/global-helpers/controller/) — The controller() function creates and returns a controller object with a custom name and optional parameters. It is prim
- [`deobfuscateParam()`](/v3-0-0/global-helpers/deobfuscateparam/) — Converts an obfuscated string back into its original value. This is typically used when IDs or other sensitive data are 
- [`distanceOfTimeInWords()`](/v3-0-0/global-helpers/distanceoftimeinwords/) — Pass in two dates to this method, and it will return a string describing the difference between them.
- [`excerpt()`](/v3-0-0/global-helpers/excerpt/) — excerpt() extracts a portion of text surrounding the first instance of a given phrase. This is useful for previews, sear
- [`humanize()`](/v3-0-0/global-helpers/humanize/) — Converts a camel-cased or underscored string into more readable, human-friendly text by inserting spaces and capitalizin
- [`hyphenize()`](/v3-0-0/global-helpers/hyphenize/) — Converts camelCase or PascalCase strings into lowercase hyphen-separated strings. Useful for generating URL-friendly slu
- [`mimeTypes()`](/v3-0-0/global-helpers/mimetypes/) — Returns the associated MIME type for a given file extension. Useful when serving files dynamically or setting response h
- [`model()`](/v3-0-0/global-helpers/model/) — Returns a reference to a specific model defined in your application, allowing you to call class-level methods on it. Thi
- [`obfuscateParam()`](/v3-0-0/global-helpers/obfuscateparam/) — Obfuscates a value, typically used to hide sensitive information like primary key IDs when passing them in URLs. This he
- [`pluginNames()`](/v3-0-0/global-helpers/pluginnames/) — Returns a list of all installed Wheels plugins in your application. This can be useful if you want to check for the pres
- [`pluralize()`](/v3-0-0/global-helpers/pluralize/) — Returns the plural form of the passed in word. Can also pluralize a word based on a value passed to the <code>count</cod
- [`singularize()`](/v3-0-0/global-helpers/singularize/) — Converts a plural word into its singular form. It uses Wheels’ built-in inflection rules, handling common English plural
- [`timeAgoInWords()`](/v3-0-0/global-helpers/timeagoinwords/) — Returns a human-friendly string describing the approximate time difference between two dates (defaults to comparing agai
- [`timeUntilInWords()`](/v3-0-0/global-helpers/timeuntilinwords/) — Returns a human-readable string describing the approximate time difference between the current date (or another starting
- [`titleize()`](/v3-0-0/global-helpers/titleize/) — Converts a string so that the first letter of each word is capitalized, producing a cleaner, title-like appearance. It i
- [`truncate()`](/v3-0-0/global-helpers/truncate/) — Shortens a given text string to a specified length and appends a replacement string (by default "...") at the end to ind
- [`URLFor()`](/v3-0-0/global-helpers/urlfor/) — Generates an internal URL based on the supplied arguments. It can create URLs using a named route, or by specifying a co
- [`wordTruncate()`](/v3-0-0/global-helpers/wordtruncate/) — Truncates text to the specified length of words and replaces the remaining characters with the specified truncate string
