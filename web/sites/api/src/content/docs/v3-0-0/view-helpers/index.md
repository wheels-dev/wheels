---
title: View Helpers
description: "59 View Helpers functions in Wheels 3.0.0."
---

- [`authenticityTokenField()`](/v3-0-0/view-helpers/authenticitytokenfield/) — Generates a hidden form field that contains a CSRF authenticity token. This token is required for verifying that POST, P
- [`autoLink()`](/v3-0-0/view-helpers/autolink/) — Scans a block of text for URLs and/or email addresses and automatically converts them into clickable links. This helper 
- [`buttonTag()`](/v3-0-0/view-helpers/buttontag/) — Builds and returns a string containing a button form control for use in your HTML forms. Use this helper to create butto
- [`buttonTo()`](/v3-0-0/view-helpers/buttonto/) — Creates a form containing a single button that submits to a URL. The URL is constructed the same way as linkTo(). This h
- [`checkBox()`](/v3-0-0/view-helpers/checkbox/) — Builds and returns a string containing a check box form control based on the supplied name.
- [`checkBoxTag()`](/v3-0-0/view-helpers/checkboxtag/) — Builds and returns a string containing a check box form control based on the supplied name.
- [`contentFor()`](/v3-0-0/view-helpers/contentfor/) — contentFor() is used to store a section's output in a layout. It allows you to define content in your view templates and
- [`contentForLayout()`](/v3-0-0/view-helpers/contentforlayout/) — contentForLayout() is used to render the main content of the current view inside a layout. In Wheels, when a controller 
- [`csrfMetaTags()`](/v3-0-0/view-helpers/csrfmetatags/) — The csrfMetaTags() helper generates meta tags containing your application's CSRF authenticity token. This is useful for 
- [`cycle()`](/v3-0-0/view-helpers/cycle/) — cycle() is a view helper used to loop through a list of values sequentially, returning the next value each time it’s cal
- [`dateSelect()`](/v3-0-0/view-helpers/dateselect/) — Builds and returns a string containing three <code>select</code> form controls for month, day, and year based on the sup
- [`dateSelectTags()`](/v3-0-0/view-helpers/dateselecttags/) — dateSelectTags() is similar to dateSelect(), but instead of binding to a model object, it works directly with a name and
- [`dateTimeSelect()`](/v3-0-0/view-helpers/datetimeselect/) — Builds and returns a string containing six <code>select</code> form controls (three for date selection and the remaining
- [`dateTimeSelectTags()`](/v3-0-0/view-helpers/datetimeselecttags/) — Builds and returns a string containing six <code>select</code> form controls (three for date selection and the remaining
- [`daySelectTag()`](/v3-0-0/view-helpers/dayselecttag/) — Builds and returns a string containing a <code>select</code> form control for the days of the week based on the supplied
- [`endFormTag()`](/v3-0-0/view-helpers/endformtag/) — Builds and returns a string containing the closing <code>form</code> tag. It’s typically used in conjunction with startF
- [`errorMessageOn()`](/v3-0-0/view-helpers/errormessageon/) — Returns the error message, if one exists, on the object's property.
- [`errorMessagesFor()`](/v3-0-0/view-helpers/errormessagesfor/) — Builds and returns a list (<code>ul</code> tag with a default <code>class</code> of <code>error-messages</code>) contain
- [`fileField()`](/v3-0-0/view-helpers/filefield/) — Builds and returns a string containing a file field form control based on the supplied objectName and property.
- [`fileFieldTag()`](/v3-0-0/view-helpers/filefieldtag/) — Builds and returns a string containing a file form control based on the supplied name.
- [`flashMessages()`](/v3-0-0/view-helpers/flashmessages/) — The flashMessages() function generates a formatted HTML output of messages stored in the Flash scope. It is typically us
- [`hasManyCheckBox()`](/v3-0-0/view-helpers/hasmanycheckbox/) — The hasManyCheckBox() helper generates the correct form elements for managing a hasMany or many-to-many association. It 
- [`hasManyRadioButton()`](/v3-0-0/view-helpers/hasmanyradiobutton/) — This helper generates radio buttons for managing a hasMany or one-to-many association, where you want the user to pick o
- [`hiddenField()`](/v3-0-0/view-helpers/hiddenfield/) — The hiddenField() function generates a hidden &lt;input type="hidden"&gt; tag for a given model object and property. It’
- [`hiddenFieldTag()`](/v3-0-0/view-helpers/hiddenfieldtag/) — Generates a hidden &lt;input type="hidden"&gt; tag using a plain name/value pair. Unlike hiddenField(), this helper does
- [`highlight()`](/v3-0-0/view-helpers/highlight/) — Searches the given text for one or more phrases and wraps all matches in an HTML tag (default: &lt;span&gt;). This is us
- [`hourSelectTag()`](/v3-0-0/view-helpers/hourselecttag/) — Builds and returns a &lt;select&gt; form control for choosing an hour of the day. By default, hours are shown in 24-hour
- [`imageTag()`](/v3-0-0/view-helpers/imagetag/) — Returns an <code>img</code> tag.
- [`includeContent()`](/v3-0-0/view-helpers/includecontent/) — Outputs the content for a specific section in a layout. Works together with contentFor() to define and then inject conte
- [`includedInObject()`](/v3-0-0/view-helpers/includedinobject/) — Used as a shortcut to check if the specified IDs are a part of the main form object.
- [`includeLayout()`](/v3-0-0/view-helpers/includelayout/) — Includes the contents of another layout file. Typically used when a child layout wants to include a parent layout, or to
- [`includePartial()`](/v3-0-0/view-helpers/includepartial/) — Includes the specified partial file in the view.
- [`javaScriptIncludeTag()`](/v3-0-0/view-helpers/javascriptincludetag/) — Generates &lt;script&gt; tags for including JavaScript files. Can handle local files in the javascripts folder or extern
- [`linkTo()`](/v3-0-0/view-helpers/linkto/) — Creates a link to another page in your application.
- [`mailTo()`](/v3-0-0/view-helpers/mailto/) — Creates a <code>mailto</code> link tag to the specified email address, which is also used as the name of the link unless
- [`minuteSelectTag()`](/v3-0-0/view-helpers/minuteselecttag/) — Builds and returns a &lt;select&gt; dropdown for the minutes of an hour (0–59). You can customize the selected value, in
- [`monthSelectTag()`](/v3-0-0/view-helpers/monthselecttag/) — Generates a &lt;select&gt; dropdown for selecting a month. You can customize its options, labels, and display format. Un
- [`paginationLinks()`](/v3-0-0/view-helpers/paginationlinks/) — Builds and returns a string containing links to pages based on a paginated query.
- [`passwordField()`](/v3-0-0/view-helpers/passwordfield/) — Builds and returns a string containing a password field form control based on the supplied objectName and property.
- [`passwordFieldTag()`](/v3-0-0/view-helpers/passwordfieldtag/) — Builds and returns a string containing a password field form control based on the supplied name.
- [`radioButton()`](/v3-0-0/view-helpers/radiobutton/) — Generates an HTML radio button for a form, based on a model object’s property. It can handle simple properties as well a
- [`radioButtonTag()`](/v3-0-0/view-helpers/radiobuttontag/) — Generates a standard HTML &lt;input type="radio"&gt; element based on the supplied name and value. Unlike radioButton(),
- [`resetCycle()`](/v3-0-0/view-helpers/resetcycle/) — Rsets a named cycle, allowing it to start from the first value the next time it is called. In Wheels, cycle() is often u
- [`secondSelectTag()`](/v3-0-0/view-helpers/secondselecttag/) — Generates an HTML &lt;select&gt; form control populated with seconds (0–59) for a minute. You can bind it to a form para
- [`select()`](/v3-0-0/view-helpers/select/) — Builds and returns an HTML &lt;select&gt; element bound to a model object property. It automatically handles nested asso
- [`selectTag()`](/v3-0-0/view-helpers/selecttag/) — Builds an HTML &lt;select&gt; element using a name and a set of options. Unlike select(), it does not require a model ob
- [`simpleFormat()`](/v3-0-0/view-helpers/simpleformat/) — Takes plain text and converts newline and carriage return characters into HTML <code>&lt;br&gt;</code> and <code>&lt;p&g
- [`startFormTag()`](/v3-0-0/view-helpers/startformtag/) — Builds and returns an opening &lt;form&gt; tag. The form’s action URL is automatically generated following the same rule
- [`stripLinks()`](/v3-0-0/view-helpers/striplinks/) — Removes all &lt;a&gt; tags (hyperlinks) from an HTML string while preserving the inner text. This is useful when you wan
- [`stripTags()`](/v3-0-0/view-helpers/striptags/) — Removes all HTML tags from a string, leaving only the raw text content. Use this when you need to sanitize HTML by compl
- [`styleSheetLinkTag()`](/v3-0-0/view-helpers/stylesheetlinktag/) — Generates one or more &lt;link&gt; tags for including CSS stylesheets in your application. By default, it looks in the <
- [`submitTag()`](/v3-0-0/view-helpers/submittag/) — Builds and returns a string containing a submit button form control.
- [`textArea()`](/v3-0-0/view-helpers/textarea/) — Builds and returns an HTML <code>&lt;textarea&gt;</code> form control for a given model object and property. It is commo
- [`textAreaTag()`](/v3-0-0/view-helpers/textareatag/) — Builds and returns an HTML <code>&lt;textarea&gt;</code> form control based only on the supplied field name, rather than
- [`textField()`](/v3-0-0/view-helpers/textfield/) — Builds and returns an HTML text field form control that is bound to a model object and one of its properties. By default
- [`textFieldTag()`](/v3-0-0/view-helpers/textfieldtag/) — Builds and returns a string containing a text field form control based on the supplied name.
- [`timeSelect()`](/v3-0-0/view-helpers/timeselect/) — Builds and returns three select form controls for hours, minutes, and seconds, based on the supplied object name and pro
- [`timeSelectTags()`](/v3-0-0/view-helpers/timeselecttags/) — Builds and returns three <code>&lt;select&gt;</code> form controls for hours, minutes, and seconds based on the supplied
- [`yearSelectTag()`](/v3-0-0/view-helpers/yearselecttag/) — Builds and returns a string containing a <code>select</code> form control for a range of years based on the supplied nam
