---
title: View Helpers
description: "88 View Helpers functions in Wheels 4.0.0."
---

- [`authenticityTokenField()`](/v4-0-0/view-helpers/authenticitytokenfield/) — Returns a hidden form field containing a new authenticity token.
- [`autoLink()`](/v4-0-0/view-helpers/autolink/) — Turns all URLs and email addresses into links.
- [`buttonTag()`](/v4-0-0/view-helpers/buttontag/) — Builds and returns a string containing a button form control.
- [`buttonTo()`](/v4-0-0/view-helpers/buttonto/) — Creates a form containing a single button that submits to the URL. Note: Pass any additional arguments by prefixing them
- [`checkBox()`](/v4-0-0/view-helpers/checkbox/) — Builds and returns a string containing a check box form control based on the supplied name.
- [`checkBoxTag()`](/v4-0-0/view-helpers/checkboxtag/) — Builds and returns a string containing a check box form control based on the supplied name.
- [`colorField()`](/v4-0-0/view-helpers/colorfield/) — Builds and returns a string containing a color picker form control based on the supplied objectName and property.
- [`colorFieldTag()`](/v4-0-0/view-helpers/colorfieldtag/) — Builds and returns a string containing a color picker form control based on the supplied name.
- [`contentFor()`](/v4-0-0/view-helpers/contentfor/) — Used to store a section's output for rendering within a layout.
- [`contentForLayout()`](/v4-0-0/view-helpers/contentforlayout/) — Includes content for the body section, which equates to the output generated by the view template run by the request.
- [`csrfMetaTags()`](/v4-0-0/view-helpers/csrfmetatags/) — Include this in your layouts' <code>head</code> sections to include meta tags containing the authenticity token for use 
- [`cycle()`](/v4-0-0/view-helpers/cycle/) — Cycles through list values every time it is called.
- [`dateField()`](/v4-0-0/view-helpers/datefield/) — Builds and returns a string containing a date field form control based on the supplied objectName and property.
- [`dateFieldTag()`](/v4-0-0/view-helpers/datefieldtag/) — Builds and returns a string containing a date field form control based on the supplied name.
- [`dateSelect()`](/v4-0-0/view-helpers/dateselect/) — Builds and returns a string containing three <code>select</code> form controls for month, day, and year based on the sup
- [`dateSelectTags()`](/v4-0-0/view-helpers/dateselecttags/) — Builds and returns a string containing three <code>select</code> form controls (month, day, and year) based on a name an
- [`dateTimeSelect()`](/v4-0-0/view-helpers/datetimeselect/) — Builds and returns a string containing six <code>select</code> form controls (three for date selection and the remaining
- [`dateTimeSelectTags()`](/v4-0-0/view-helpers/datetimeselecttags/) — Builds and returns a string containing six <code>select</code> form controls (three for date selection and the remaining
- [`daySelectTag()`](/v4-0-0/view-helpers/dayselecttag/) — Builds and returns a string containing a <code>select</code> form control for the days of the week based on the supplied
- [`emailField()`](/v4-0-0/view-helpers/emailfield/) — Builds and returns a string containing an email field form control based on the supplied objectName and property.
- [`emailFieldTag()`](/v4-0-0/view-helpers/emailfieldtag/) — Builds and returns a string containing an email field form control based on the supplied name.
- [`endFormTag()`](/v4-0-0/view-helpers/endformtag/) — Builds and returns a string containing the closing <code>form</code> tag.
- [`errorMessageOn()`](/v4-0-0/view-helpers/errormessageon/) — Returns the error message, if one exists, on the object's property.
- [`errorMessagesFor()`](/v4-0-0/view-helpers/errormessagesfor/) — Builds and returns a list (<code>ul</code> tag with a default <code>class</code> of <code>error-messages</code>) contain
- [`fileField()`](/v4-0-0/view-helpers/filefield/) — Builds and returns a string containing a file field form control based on the supplied objectName and property.
- [`fileFieldTag()`](/v4-0-0/view-helpers/filefieldtag/) — Builds and returns a string containing a file form control based on the supplied name.
- [`firstPageLink()`](/v4-0-0/view-helpers/firstpagelink/) — Creates a link to the first page, or a disabled span when already on the first page.
- [`flashMessages()`](/v4-0-0/view-helpers/flashmessages/) — Displays a marked-up listing of messages that exist in the Flash.
- [`h()`](/v4-0-0/view-helpers/h/) — Encodes a value for safe HTML output. Use in templates to prevent XSS:
- [`hasManyCheckBox()`](/v4-0-0/view-helpers/hasmanycheckbox/) — Used as a shortcut to output the proper form elements for an association.
- [`hasManyRadioButton()`](/v4-0-0/view-helpers/hasmanyradiobutton/) — Used as a shortcut to output the proper form elements for an association.
- [`hAttr()`](/v4-0-0/view-helpers/hattr/) — Encodes a value for safe use inside an HTML attribute.
- [`hiddenField()`](/v4-0-0/view-helpers/hiddenfield/) — Builds and returns a string containing a hidden field form control based on the supplied objectName and property.
- [`hiddenFieldTag()`](/v4-0-0/view-helpers/hiddenfieldtag/) — Builds and returns a string containing a hidden field form control based on the supplied name.
- [`highlight()`](/v4-0-0/view-helpers/highlight/) — Highlights the phrase(s) everywhere in the text if found by wrapping them in <code>span</code> tags.
- [`hourSelectTag()`](/v4-0-0/view-helpers/hourselecttag/) — Builds and returns a string containing one <code>select</code> form control for the hours of the day based on the suppli
- [`imageTag()`](/v4-0-0/view-helpers/imagetag/) — Returns an <code>img</code> tag.
- [`includeContent()`](/v4-0-0/view-helpers/includecontent/) — Used to output the content for a particular section in a layout.
- [`includedInObject()`](/v4-0-0/view-helpers/includedinobject/) — Used as a shortcut to check if the specified IDs are a part of the main form object.
- [`includeLayout()`](/v4-0-0/view-helpers/includelayout/) — Includes the contents of another layout file.
- [`includePartial()`](/v4-0-0/view-helpers/includepartial/) — Includes the specified partial file in the view.
- [`javaScriptIncludeTag()`](/v4-0-0/view-helpers/javascriptincludetag/) — Returns a <code>script</code> tag for a JavaScript file (or several) based on the supplied arguments.
- [`lastPageLink()`](/v4-0-0/view-helpers/lastpagelink/) — Creates a link to the last page, or a disabled span when already on the last page.
- [`linkTo()`](/v4-0-0/view-helpers/linkto/) — Creates a link to another page in your application.
- [`mailTo()`](/v4-0-0/view-helpers/mailto/) — Creates a <code>mailto</code> link tag to the specified email address, which is also used as the name of the link unless
- [`minuteSelectTag()`](/v4-0-0/view-helpers/minuteselecttag/) — Builds and returns a string containing one <code>select</code> form control for the minutes of an hour based on the supp
- [`monthSelectTag()`](/v4-0-0/view-helpers/monthselecttag/) — Builds and returns a string containing a <code>select</code> form control for the months of the year based on the suppli
- [`nextPageLink()`](/v4-0-0/view-helpers/nextpagelink/) — Creates a link to the next page, or a disabled span when on the last page.
- [`numberField()`](/v4-0-0/view-helpers/numberfield/) — Builds and returns a string containing a number field form control based on the supplied objectName and property.
- [`numberFieldTag()`](/v4-0-0/view-helpers/numberfieldtag/) — Builds and returns a string containing a number field form control based on the supplied name.
- [`pageNumberLinks()`](/v4-0-0/view-helpers/pagenumberlinks/) — Creates a windowed set of page number links around the current page.
- [`paginationInfo()`](/v4-0-0/view-helpers/paginationinfo/) — Displays a text summary of the current pagination state, e.g. "Showing 26-50 of 1,000 records".
- [`paginationLinks()`](/v4-0-0/view-helpers/paginationlinks/) — Builds and returns a string containing links to pages based on a paginated query.
- [`paginationNav()`](/v4-0-0/view-helpers/paginationnav/) — Creates a complete pagination navigation element wrapping individual pagination helpers.
- [`passwordField()`](/v4-0-0/view-helpers/passwordfield/) — Builds and returns a string containing a password field form control based on the supplied objectName and property.
- [`passwordFieldTag()`](/v4-0-0/view-helpers/passwordfieldtag/) — Builds and returns a string containing a password field form control based on the supplied name.
- [`previousPageLink()`](/v4-0-0/view-helpers/previouspagelink/) — Creates a link to the previous page, or a disabled span when on the first page.
- [`radioButton()`](/v4-0-0/view-helpers/radiobutton/) — Builds and returns a string containing a radio button form control based on the supplied objectName and property.
- [`radioButtonTag()`](/v4-0-0/view-helpers/radiobuttontag/) — Builds and returns a string containing a radio button form control based on the supplied name.
- [`rangeField()`](/v4-0-0/view-helpers/rangefield/) — Builds and returns a string containing a range slider form control based on the supplied objectName and property.
- [`rangeFieldTag()`](/v4-0-0/view-helpers/rangefieldtag/) — Builds and returns a string containing a range slider form control based on the supplied name.
- [`resetCycle()`](/v4-0-0/view-helpers/resetcycle/) — Resets a cycle so that it starts from the first list value the next time it is called.
- [`searchField()`](/v4-0-0/view-helpers/searchfield/) — Builds and returns a string containing a search field form control based on the supplied objectName and property.
- [`searchFieldTag()`](/v4-0-0/view-helpers/searchfieldtag/) — Builds and returns a string containing a search field form control based on the supplied name.
- [`secondSelectTag()`](/v4-0-0/view-helpers/secondselecttag/) — Builds and returns a string containing one <code>select</code> form control for the seconds of a minute based on the sup
- [`select()`](/v4-0-0/view-helpers/select/) — Builds and returns a string containing a select form control based on the supplied objectName and property.
- [`selectTag()`](/v4-0-0/view-helpers/selecttag/) — Builds and returns a string containing a select form control based on the supplied name and options.
- [`simpleFormat()`](/v4-0-0/view-helpers/simpleformat/) — Returns formatted text using HTML break tags (<code><br></code>) and HTML paragraph elements (<code><p></p></code>) base
- [`startFormTag()`](/v4-0-0/view-helpers/startformtag/) — Builds and returns a string containing the opening <code>form</code> tag.
- [`stripLinks()`](/v4-0-0/view-helpers/striplinks/) — Removes all links from an HTML string, leaving just the link text.
- [`stripTags()`](/v4-0-0/view-helpers/striptags/) — Removes all HTML tags from a string.
- [`styleSheetLinkTag()`](/v4-0-0/view-helpers/stylesheetlinktag/) — Returns a <code>link</code> tag for a stylesheet (or several) based on the supplied arguments.
- [`submitTag()`](/v4-0-0/view-helpers/submittag/) — Builds and returns a string containing a submit button form control.
- [`telField()`](/v4-0-0/view-helpers/telfield/) — Builds and returns a string containing a telephone field form control based on the supplied objectName and property.
- [`telFieldTag()`](/v4-0-0/view-helpers/telfieldtag/) — Builds and returns a string containing a telephone field form control based on the supplied name.
- [`textArea()`](/v4-0-0/view-helpers/textarea/) — Builds and returns a string containing a text area field form control based on the supplied objectName and property.
- [`textAreaTag()`](/v4-0-0/view-helpers/textareatag/) — Builds and returns a string containing a text area form control based on the supplied name.
- [`textField()`](/v4-0-0/view-helpers/textfield/) — Builds and returns a string containing a text field form control based on the supplied objectName and property.
- [`textFieldTag()`](/v4-0-0/view-helpers/textfieldtag/) — Builds and returns a string containing a text field form control based on the supplied name.
- [`timeSelect()`](/v4-0-0/view-helpers/timeselect/) — Builds and returns a string containing three <code>select</code> form controls for hour, minute, and second based on the
- [`timeSelectTags()`](/v4-0-0/view-helpers/timeselecttags/) — Builds and returns a string containing three <code>select</code> form controls for hour, minute, and second based on nam
- [`urlField()`](/v4-0-0/view-helpers/urlfield/) — Builds and returns a string containing a URL field form control based on the supplied objectName and property.
- [`urlFieldTag()`](/v4-0-0/view-helpers/urlfieldtag/) — Builds and returns a string containing a URL field form control based on the supplied name.
- [`viteAsset()`](/v4-0-0/view-helpers/viteasset/) — Returns the resolved URL for a Vite entrypoint. In production, reads the Vite manifest
- [`vitePreloadTag()`](/v4-0-0/view-helpers/vitepreloadtag/) — Returns <code><link rel="modulepreload"></code> tags for a Vite entrypoint and its transitive
- [`viteScriptTag()`](/v4-0-0/view-helpers/vitescripttag/) — Returns 'script' tags for a Vite JS entrypoint. In development, also injects the Vite
- [`viteStyleTag()`](/v4-0-0/view-helpers/vitestyletag/) — Returns a <code><link></code> tag for a Vite CSS entrypoint. In development, Vite injects CSS via
- [`yearSelectTag()`](/v4-0-0/view-helpers/yearselecttag/) — Builds and returns a string containing a <code>select</code> form control for a range of years based on the supplied nam
