/**
 * Create various Wheels components
 * @help Generate models, controllers, views, tests, and more
 * @namespace true
 */
component excludeFromHelp=false {
    
    // This is a namespace command - subcommands are in the create/ directory
    // CommandBox will automatically handle subcommand routing
    
}