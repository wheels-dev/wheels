/**
 * Base wheels command - use subcommands like 'wheels create app'
 * @help The Wheels CLI provides commands for creating and managing Wheels applications
 * @namespace true
 */
component excludeFromHelp=false {
    
    // Namespace command - no run() method needed
    
}