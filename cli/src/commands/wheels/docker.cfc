/**
 * Manage Docker environments for Wheels applications
 * 
 * This is a namespace for docker-related commands.
 * Use 'wheels docker help' to see available subcommands.
 */
component extends="base" {
    // Parent namespace command - no run() method needed
    // Subcommands will be automatically discovered
}