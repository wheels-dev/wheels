/**
 * Docker test commands for templates and examples
 * 
 * This is a namespace for docker test-related commands.
 * Use 'wheels docker test help' to see available subcommands.
 */
component extends="../base" {
    // Parent namespace command - no run() method needed
    // Subcommands will be automatically discovered
}