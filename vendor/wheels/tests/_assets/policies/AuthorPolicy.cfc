/**
 * Test fixture policy for the Author model with NO overrides — every action and
 * the scope inherit the DEFAULT-DENY behavior from the wheels.Policy base, so
 * specs can pin the base-class contract through a real app-style policy.
 */
component extends="Policy" {


}
