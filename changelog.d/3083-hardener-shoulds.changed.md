- `wheels browser test` now fails closed on compile-skipped `*Spec.cfc` files (same unloadable probe as `wheels test`); the unloadable WARN no longer implies a soft-fail. Legacy CommandBox `wheels test` / `browser:test` refuse with a deprecation error instead of swallowing TestBox failing exit codes. Unrouted `vendor/wheels/controllers/Tests.cfc` is removed — `/wheels/core/tests` and `/wheels/app/tests` stay on `Public.cfc` (#3083)
