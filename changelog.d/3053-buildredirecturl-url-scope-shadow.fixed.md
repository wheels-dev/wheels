- Fixed every password-gated reload (`?reload=true&password=...`) and URL environment switch returning HTTP 500 on Adobe ColdFusion — `$buildRedirectUrl()` in `public/Application.cfc` declared a string `local.url` that shadowed the reserved `url` scope, so the `#3036` environment-switch reads threw a `ScopeCastException` before `applicationStop()`. The local is renamed to `local.redirectUrl` so the `url.*` reads resolve to the URL scope on every engine (#3053)
