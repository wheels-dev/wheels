- `box install wheels-starter-app` no longer aborts with "Error getting ForgeBox entry [wheels-authenticateThis] — the entry slug sent is invalid or does not exist". The starter app's `box.json` declared the unpublished `wheels-authenticateThis` slug as a ForgeBox dependency even though the plugin already ships bundled under `examples/starter-app/plugins/authenticateThis/`; the broken dependency (and its stale `installPaths` entry) is dropped so `wheels-core` is the only fetched dependency and the bundled plugin is used instead. `flashMessages()` is already in core, so no other plugin needs fetching (#3181)
