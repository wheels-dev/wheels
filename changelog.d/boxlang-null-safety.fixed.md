- Hardened cross-engine null handling in the router, dispatcher, and error handler for BoxLang, whose stricter semantics throw a `NullPointerException` on a null string/struct subject where Lucee and Adobe coerce null to `""`/empty. `mapper/matching.cfc` now normalizes a null derived `pattern` to `""` (a named route given via `to=` with no explicit pattern previously failed to load on BoxLang); `Dispatch.cfc` `$buildMiddlewareCgiScope()` defaults null request headers to an empty struct; and `EventMethods.cfc` `$runOnError()` guards a null exception `cause`. New `mapperNullPatternSpec` exercises the route shapes (named `to=` and `resources()`) that the compat-matrix demo app's `.wildcard().root()` routes left uncovered on BoxLang
