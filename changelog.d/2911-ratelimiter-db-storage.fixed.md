- RateLimiter database storage hardened for multi-node use: dialect-aware atomic counter upsert (MySQL/MariaDB, PostgreSQL, SQLite) with unique-constraint-backed insert-retry everywhere else, a v2 `wheels_rate_limits` schema (UNIQUE `store_key`, `row_type` discriminator, `client_key` lookup column), transaction + row-lock (`FOR UPDATE` / `UPDLOCK`) serialization for the slidingWindow and tokenBucket read-modify-write paths, and an automatic one-time upgrade of the auto-created table from the legacy schema (#2911)
- RateLimiter tokenBucket with storage=database now enforces correctly on engines where SQLite returns timestamps as raw epoch milliseconds (observed on Adobe ColdFusion + sqlite-jdbc) — the refill calculation previously read every bucket as fully refilled and never blocked (#2911)
