# This workflow is used to release master releases and also by the snapshot workflow
# to release snapshots.
name: Wheels Release

on:
  push:
    branches:
      - main
  workflow_call:
    secrets:
      SLACK_WEBHOOK_URL:
        required: true
      FORGEBOX_API_TOKEN:
        required: true
      FORGEBOX_PASS:
        required: true

env:
  WHEELS_PRERELEASE: false

jobs:
  #############################################
  # Build Snapshot or Final Release
  #############################################
  build:
    name: Build & Publish Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        id: Checkout-Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Environment Variables For Build Process
        id: Calculate-Version-And-Branch
        run: |
          cat box.json
          echo "WHEELS_VERSION=`cat box.json | jq '.version' -r`+${{ github.run_number }}" >> $GITHUB_ENV
          # main or snapshot
          echo "BRANCH=main" >> $GITHUB_ENV
          if [ $GITHUB_REF == 'refs/heads/develop' ]
          then
            echo "BRANCH=develop" >> $GITHUB_ENV
          fi

      - name: Build Wheels Variants for ${{ env.BRANCH }} v${{ env.WHEELS_VERSION }}
        id: Build-Wheels-Variants
        run: |
          ant -DisPreRelease=${{ env.WHEELS_PRERELEASE }} -Dwheels.version=${{ env.WHEELS_VERSION }} -Dbuild.branch=${{ env.BRANCH }} -Dbuild.number=${{ github.run_number }} -f build/build.xml

      - name: Upload Wheels Core Artifact
        id: Upload-Wheels-Core
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: wheels-core
          path: |
            artifacts/wheels/${{ env.WHEELS_VERSION }}/wheels-*.zip
            artifacts/wheels/${{ env.WHEELS_VERSION }}/wheels-*.md5
            artifacts/wheels/${{ env.WHEELS_VERSION }}/wheels-*.sha512
            artifacts/wheels/wheels-be.zip

      - name: Upload Wheels Base Template Artifact
        id: Upload-Wheels-Base
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: wheels-base-template
          path: |
            artifacts/wheels/${{ env.WHEELS_VERSION }}/wheels-base-template-*.zip
            artifacts/wheels/${{ env.WHEELS_VERSION }}/wheels-base-template-*.md5
            artifacts/wheels/${{ env.WHEELS_VERSION }}/wheels-base-template-*.sha512
            artifacts/wheels/wheels-base-template-be.zip

      - name: Upload Wheels CLI Artifact
        id: Upload-Wheels-CLI
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: wheels-cli
          path: |
            artifacts/wheels/${{ env.WHEELS_VERSION }}/wheels-cli-*.zip
            artifacts/wheels/${{ env.WHEELS_VERSION }}/wheels-cli-*.md5
            artifacts/wheels/${{ env.WHEELS_VERSION }}/wheels-cli-*.sha512
            artifacts/wheels/wheels-cli-be.zip

      - name: Upload All Wheels Artifacts (Combined)
        id: Upload-All-Artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: wheels-all-variants
          path: |
            artifacts/**/*

      # publish core package
      - uses: pixl8/github-action-box-publish@v4
        id: Publish-Core-Package
        with:
          forgebox_user: wheels-dev
          forgebox_pass: ${{ secrets.FORGEBOX_PASS }}
          force: "true"
        env:
          BOXJSON_DIR: /build-cfwheels-core/cfwheels

      # publish base package
      - uses: pixl8/github-action-box-publish@v4
        id: Publish-Base-Template
        with:
          forgebox_user: wheels-dev
          forgebox_pass: ${{ secrets.FORGEBOX_PASS }}
          force: "true"
        env:
          BOXJSON_DIR: /build-cfwheels-base/cfwheels

      # publish CLI package
      - uses: pixl8/github-action-box-publish@v4
        id: Publish-CLI-Package
        with:
          forgebox_user: wheels-dev
          forgebox_pass: ${{ secrets.FORGEBOX_PASS }}
          force: "true"
        env:
          BOXJSON_DIR: /build-cfwheels-cli/wheels-cli

#      - name: Post Slack Message
#        id: Post-Slack-Message
#        uses: act10ns/slack@v2
#        with:
#          webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
#          status: ${{ job.status }}
#          steps: ${{ toJson(steps) }}
#          channel: '#wheels-dev'
#        if: always()
